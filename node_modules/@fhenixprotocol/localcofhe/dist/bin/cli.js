#!/usr/bin/env node
import { localCofheUp, localCofheDown, localCofhePull } from '../src/core.js';
const args = process.argv.slice(2);
if (!args.length || args[0] === 'up') {
    localCofheUp();
}
else if (args[0] === 'down') {
    localCofheDown();
}
else if (args[0] === 'pull') {
    localCofhePull();
}
else {
    console.error('Unknown command.');
    process.exit(1);
}
//# sourceMappingURL=cli.js.map